# 后台点击更新程序

## 工作方式

GitHub 默认分支更新 → Actions 使用 PHP 8.5 测试 → 发布代码包到 GitHub Releases → 管理员登录后台点击“检查更新” → 确认“更新程序” → 本地服务下载、验证、切换、检查健康状态。不会提交后自动上线，也不需要服务器公网 SSH。

程序后台仍为 PHP；本地更新服务使用系统 Python 3.9+ 标准库，以 ossdeploy 非 root 用户运行。它只从固定的公开仓库 `52okp/52okp-oss` 获取正式发布包。无需 Python第三方库、Composer、SSH私钥或新的公网服务端口。

## 现有站点一次性安装

账号初始化已完成，不要再运行 init.php，不覆盖 shared。

1. 先备份 `/www/wwwroot/app_52okp_com/shared`。在宝塔终端确认 `/usr/bin/python3 --version` 为3.9或以上、`systemctl --version` 可用。确认服务器可以通过 HTTPS 访问 api.github.com、github.com 及 GitHub 发布资产的重定向下载域名。
2. 创建新目录 `/www/wwwroot/app_52okp_com/releases/2026091702-1`。将“网页更新安装包.zip”上传到该目录，解压后目录直接包含 public、src、bin、deploy、tests、config.example.php、README.md；不要多套 app 或压缩包名称目录，不覆盖原来的 `2026091701-1`。
3. 确保新目录及代码可被 oss-files 组读取。手动上传的目录通常是 www:www、0755，可读取；不得使用777。网站根目录继续为 `/www/wwwroot/app_52okp_com/current/public`。
4. 在宝塔 root 终端执行以下一条命令（复制完整一行）：

   ```bash
   bash /www/wwwroot/app_52okp_com/releases/2026091702-1/deploy/bootstrap-web-update.sh /www/wwwroot/app_52okp_com ossdeploy www /www/server/php/85/bin/php https://app.52okp.com/health
   ```

   此脚本检查新代码，安装服务，原子切换 current 并验证新版本标识。失败时尝试切回原代码，不重置账号，不写入上传/文件元数据/会话目录。脚本要求当前版本与新版本目录不同；重复执行会停止，而不是覆盖运行代码。服务安装需一次 root 权限，日常更新不需要root。
5. 若刚修改了系统组权限，重启 PHP 8.5 和 Nginx。保持原先 PHP 的 OSS_CONFIG FastCGI 参数，以及 Nginx 的内部下载目录配置。
6. EdgeOne 保持 HTTP 回源；公网必须 HTTPS。后台 `/`（包括 `?api=updates`）、`/index.php`、`/health`、`/d/*` 和 POST 请求绕过缓存。静态资源保留查询参数参与缓存键；必要时清理旧 CSS/JS 缓存。
7. 登录 `https://app.52okp.com`，出现“程序更新”区。点击“检查更新”；状态显示最新 build 标识后，点击“更新程序”并确认。页面定期读取状态，但不会自动检查 GitHub 或自动上线。成功后点击刷新页面。

更新区显示任务阶段进度、任务编号、耗时以及成功/失败反馈。查询GitHub时采用动态进度，不编造下载百分比；安装按下载校验、代码测试、切换与健康检查显示阶段进度。断网时会明确提示“状态同步失败”，不把未知结果误报为更新失败；可点击“刷新状态”。即使JavaScript未启用，提交后刷新页面也能看到阶段和结果。此功能兼容已安装的协议1更新服务，无需重装systemd服务。

若提示“GitHub 暂无可用发布包”，检查仓库 Actions 的 Validate and publish update 流程是否完成。GitHub Actions有失败时不发布包；未发布的草稿包不可安装。

## 更新的安全边界

- 管理请求必须登录、CSRF验证，安装还需明确点击确认。同一时间只允许一个任务，独立文件锁防并发。
- PHP仅能写共享队列，不能写项目根目录和新发布目录；真正发布由 ossdeploy 本地服务执行。
- 只接受固定仓库正式 Releases 的 `build-数字-数字` 标签，不接受任意 URL、分支、路径或 shell命令。
- 发布资产使用 [GitHub API 提供的 SHA-256 digest](https://docs.github.com/en/rest/releases/assets#get-a-release-asset) 验证，下载和重定向必须HTTPS。校验用于证明包与GitHub资产一致，并非独立签名；GitHub仓库和具有发布权限的账号是信任边界，应开启2FA和默认分支保护。
- 发布包仅包含程序目录，大小上限20 MiB，展开总大小64 MiB、最多1000项。拒绝路径穿越、shared目录、符号链接、硬链接、重复文件、设备和稀疏文件。
- PHP语法和基础测试通过后再切换；health不仅要求ok，还核对新版本标识，避免CDN旧响应误判。
- 更新期间共享上传数据和账号保持不变。服务受 systemd 限制，配置、上传、文件元数据及会话目录只读。
- 切换前持久化回滚日志；普通失败立即回退，进程被强杀或断电后服务重新启动时依据日志恢复。磁盘/硬件损坏或服务无法重新启动仍可能需人工介入，不承诺所有故障都能自动恢复。
- root管理的更新服务文件不会跟随网页发布包自动覆盖。未来更新协议变化时，需人工运行新版 install-updater.sh 更新服务；当前协议为1。

## 排错、日志、回滚

```bash
systemctl status 52okp-oss-updater --no-pager
journalctl -u 52okp-oss-updater -n 80 --no-pager
curl -fsS --max-time 15 https://app.52okp.com/health
```

服务卡住或重启后仍失败，先看日志，不手工清空队列或回滚日志。服务器不能访问 GitHub 时更新失败，运行版本不受影响。

服务默认保留最近5个经本服务健康检查成功的发布，以及当前和上一版本。失败发布保留用于排错，手动上传的旧代码不会自动清理；应定期检查磁盘。所有清理仅针对验证过的代码目录，不清理 shared。

人工回滚：先停止服务 `systemctl stop 52okp-oss-updater`，确认没有正在运行的任务，选择已验证的旧 release，创建临时符号链接并 `mv -Tf` 原子替换 current，验证health、登录和下载。不要覆盖或回滚shared。恢复服务前检查 update-cache/journal.json 是否存在，避免待恢复任务覆盖人工选择；若存在先保留日志并按其previous目标恢复。

旧 SSH 工作流已替换，不再使用 DEPLOY_ENABLED 或 production 中的 SSH_* Secrets。可在确认本地更新成功后自行清理旧Secrets和部署公钥，暂不需要删除以免误操作。
